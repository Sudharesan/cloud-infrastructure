# Usage notes

This docs folder explains how to use the example templates in this repository.

- `terraform/aws/` contains a minimal S3 example. Inspect and customize `main.tf` before running.
- `k8s/` contains deployment and service manifests for sample applications.
- CI workflows in `ci/` demonstrate linting and basic CI for IaC.
